apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "genesis-generator.fullname" . }}-input
  labels:
    {{- include "genesis-generator.labels" . | nindent 4 }}
data:
  values.env: |
    {{- range $key, $value := .Values.chainConfig }}
    export {{ $key }}={{ $value | quote }}
    {{- end }}
  mnemonics.yaml: |
    {{- .Values.mnemonics | toYaml | nindent 4 }}
