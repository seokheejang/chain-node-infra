apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "lighthouse.fullname" . }}
  labels:
    {{- include "lighthouse.labels" . | nindent 4 }}
data:
  network: {{ .Values.config.network | quote }}
  execution-endpoint: {{ .Values.config.executionEndpoint | quote }}
